//Write a program to add two number using functions

#include <stdio.h>
int Add(int ,int);
int main()
{
    int a,b;
    a=20;
    b=30;
    int sum=Add(a,b);
    printf("Sum is %d",sum);
    return 0;
}
int Add(int a1,int n1)
{
    return (a1+n1);
}
