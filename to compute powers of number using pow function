#include <stdio.h>
#include<math.h>
int main()
{
int base;
float exponent;
float  result;
printf("Enter base element:");
scanf("%d",&base);
printf("Enter exponent element:");
scanf("%f",&exponent);

result=pow(base,exponent);
printf("Power of %d is %f",base,result);
return 0;
}
